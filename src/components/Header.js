import Alert from 'react-bootstrap/Alert';
const Header = () =>  {
    return (
        <div>
            <Alert variant={'primary'}>
                Pokemon Battle
            </Alert>
        </div>
    );
}

export default Header;